<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.Ex02.mapper.UserMapper">

<<<<<<< HEAD
    <!-- 유저 회원가입 정보   -->
    <insert id="insertUser">
        INSERT INTO USERS (
        EMAIL,
        PASSWORD,
        NICKNAME,
        BIRTHDATE,
        HEIGHT,
        CURRENT_WEIGHT,
        MAIN_GOAL
        )
        VALUES (
        #{email},
        #{password},
        #{nickname},
        #{birthdate},
        #{height},
        #{currentWeight},
        #{mainGoal}
        )
    </insert>

    <!-- 이메일 중복 체크 -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
=======
    <resultMap id="userMap" type="com.example.Ex02.dto.UserJoinDto">
        <id column="USER_ID" property="userId"/>
        <result column="EMAIL" property="email"/>
        <result column="PASSWORD" property="password"/>
        <result column="NICKNAME" property="nickname"/>
        <result column="HEIGHT" property="height"/>
        <result column="CURRENT_WEIGHT" property="currentWeight"/>
        <result column="MAIN_GOAL" property="mainGoal"/>
        <result column="PROFILE_IMAGE_URL" property="profileImageUrl"/>
    </resultMap>
    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="com.example.Ex02.dto.UserJoinDto">

        <selectKey keyProperty="userId" resultType="long" order="BEFORE">
            SELECT USERS_SEQ.NEXTVAL FROM DUAL
        </selectKey>

        INSERT INTO USERS (
        USER_ID,
        EMAIL,
        PASSWORD,
        NICKNAME,
        HEIGHT,
        CURRENT_WEIGHT,
        MAIN_GOAL,
        PROFILE_IMAGE_URL
        ) VALUES (
        #{userId},
        #{email},
        #{password},
        #{nickname},
        #{height},
        #{currentWeight},
        #{mainGoal},
        #{profileImageUrl}
        )

    </insert>

    <!-- 이메일로 조회 -->
    <select id="findByEmail" resultMap="userMap">
        SELECT *
>>>>>>> home2
        FROM USERS
        WHERE EMAIL = #{email}
    </select>

<<<<<<< HEAD
    <!-- 로그인: 이메일로 회원 정보 조회 -->
    <select id="findByEmail"
            parameterType="string"
            resultType="com.example.Ex02.dto.UserDto">

        SELECT
        USER_ID,
        EMAIL,
        PASSWORD_HASH AS password,
        NICKNAME,
        BIRTHDATE,
        HEIGHT,
        CURRENT_WEIGHT,
        MAIN_GOAL
        FROM USERS
        WHERE EMAIL = #{email}

    </select>

    <!-- 마이페이지 : 사용자 상세 정보 띄우기  -->
    <select id="findById" parameterType="long" resultType="com.example.Ex02.dto.UserDto">
=======
    <!-- ID로 조회 -->
    <select id="findById" resultMap="userMap">
>>>>>>> home2
        SELECT *
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

<<<<<<< HEAD
</mapper>
=======
    <!-- 이메일 중복 체크 -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM USERS WHERE EMAIL = #{email}
    </select>


</mapper>
>>>>>>> home2
